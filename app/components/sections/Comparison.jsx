// Section removed per client report RNO-RP213 ("Éléments et sections à éviter").
// Kept as an empty module so any stale import fails loudly rather than silently
// rendering the old "4 000 DH / -80 %" block. Safe to delete this file.
export default function Comparison() {
  return null;
}
