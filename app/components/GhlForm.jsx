// Removed — the page uses the in-house <LeadForm /> again, which posts to the
// n8n webhook. Kept as a stub in case the wepushx embed is needed later; the
// original iframe markup is in git history.
export default function GhlForm() {
  return null;
}
